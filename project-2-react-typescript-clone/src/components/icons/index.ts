/**
 * Semantic icon set, mapped onto the Phosphor icon family (the same set the
 * original Fermo template uses). Re-exporting here keeps icon choices in one
 * place and lets the rest of the app import by intent rather than by glyph.
 */
export {
  PaintBrush as PaintBrushIcon,
  Brain as BrainIcon,
  City as CityIcon,
  PlugsConnected as PlugsConnectedIcon,
  HouseLine as HomeIcon,
  Note as ExperienceIcon,
  User as AboutIcon,
  FolderSimpleUser as WorksIcon,
  ArrowUpRight as ArrowUpRightIcon,
  DownloadSimple as DownloadIcon,
  ArrowRight as ArrowRightIcon,
  CalendarBlank as CalendarIcon,
  EnvelopeSimple as EnvelopeIcon,
  Sun as SunIcon,
  Moon as MoonIcon,
} from "@phosphor-icons/react";
